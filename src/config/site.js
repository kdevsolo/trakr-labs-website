// Central place to configure the live product / dashboard destinations.
// Point these at your real auth + app URLs.
export const APP_URL = 'https://app.trakr.io'
export const SIGN_IN_URL = `${APP_URL}/login`
export const GET_STARTED_URL = `${APP_URL}/signup`

// Embed URL for the product demo video (e.g. a YouTube/Vimeo embed link).
// Leave empty to show the poster + play button without an embedded player.
export const DEMO_VIDEO_URL = ''

export const BRAND = {
  name: 'Trakr',
  company: 'Trakr Labs',
}
